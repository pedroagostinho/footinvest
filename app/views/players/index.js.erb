// app/views/players/index.js.erb
// Here you generate *JavaScript* that would be executed in the browser
function refreshForm(innerHTML) {
  const playersDiv = document.getElementsByClassName('players')[0];
  playersDiv.innerHTML = innerHTML;
}


// PRIMEIRO STEP RE-RENDER DO PARTIAL ou apagar HTML

// Por HTML com search


refreshForm('<%= j render "players/market" %>');

